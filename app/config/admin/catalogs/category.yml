easy_admin:
  entities:
    Category:
      class: AppBundle\Entity\Categoria
      label: 'Categoría'
      list:
        title: 'Categoría'
        actions:
          - { name: 'new', label: 'Crear categoria' }
        fields:
          - id
          - nombre
          - clave
      form:
        fields:
          - { property: nombre, label: 'Nombre' }
          - { property: clave, label: 'Clave' }
